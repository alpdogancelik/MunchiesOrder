// Optional seeding script copied from food_ordering-main. Requires valid Appwrite creds.
export default async function seed() {
    console.log('Seed not implemented in mobile copy. Use the original script if needed.');
}